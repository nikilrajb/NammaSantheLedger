package com.nammasanthe.ledger.ui.theme

import androidx.compose.ui.graphics.Color

// ── Namma-Santhe Ledger Colour Palette ──────────────────────────────────
// Theme: Warm market feel — saffron, deep greens, warm off-white

// Primary - Saffron (market / prosperity energy)
val Saffron700    = Color(0xFFE65100)   // Primary action buttons, FAB
val Saffron500    = Color(0xFFFF6F00)   // Active navigation, highlights
val Saffron100    = Color(0xFFFFF8F0)   // Screen backgrounds
val Saffron50     = Color(0xFFFFFDE7)   // Card surfaces

// Positive / Credit (Udari given - money going out to customer)
val CreditGreen   = Color(0xFF2E7D32)   // CREDIT transaction labels
val CreditLight   = Color(0xFFE8F5E9)   // CREDIT row background tint

// Negative / Payment (money coming in from customer)
val PaymentRed    = Color(0xFFC62828)   // PAYMENT transaction labels
val PaymentLight  = Color(0xFFFCE4EC)   // PAYMENT row background tint

// Neutral
val TextPrimary   = Color(0xFF1A237E)   // All primary text
val TextSecondary = Color(0xFF546E7A)   // Secondary / hint text
val DividerColor  = Color(0xFFE0E0E0)   // Dividers, borders
val SurfaceWhite  = Color(0xFFFFFFFF)   // Card surface, dialogs
val ScaffoldBg    = Color(0xFFFFF8F0)   // All screen backgrounds

// Semantic
val OutstandingRed= Color(0xFFB71C1C)   // Total Outstanding amount
val BalanceGreen  = Color(0xFF1B5E20)   // Positive balance indicator
val NeutralGray   = Color(0xFF757575)   // Zero balance / disabled